import React, { Component } from "react";

const withFetch = (url) => {
  return class WithFetch extends Component {
    state = {};

    // componentDidMount() {
    //   this.setState({});
    //   fetch(url).then(() => {
    //     response =>
    //   });
    // }

    render() {
      return <></>;
    }
  };
};

export default withFetch;
